import { ISPService } from "./ISPService";
const LOG_SOURCE = "SPService.ts";
import { Log } from "@microsoft/sp-core-library";

export class SPService implements ISPService {
    public async getCSVFiles(): Promise<any[]> {
    //     let newsTickers:any[];
    //     try {
            
    //         //  newsTickers = await sp.web.lists.getByTitle(listName).items.select("*").get();
    //       await 
    //     } catch (error) {
    //         Log.error(LOG_SOURCE + "getCSVFiles():", error);
    //     }
    //
         return null;
     }
}